<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
  <f:entry title="${it.name}" description="${it.description}">
    <f:textbox name="label" value="${it.value}" readonly="true" />
  </f:entry>
</j:jelly>